## Pyenv 笔记

[返回索引](../README.md)

---

```
管理 Python 解释器版本（全局/项目级）
uv是管理项目python的虚拟环境与依赖,和pyenv配合使用
* pyyenv versions  # 列出Pyhon版本
* pyvenv install <version>
* pyenv local <version>
* 针对Python版本管理生成类饿.python-version文件,不过uv可以替代这一步操作(uv init)
```
